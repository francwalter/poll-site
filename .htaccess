RewriteEngine On

# Allow direct access to poll_list.php
RewriteRule ^poll-list$ poll_list.php [L]

# Redirect requests that aren't files/folders to index.php with slug
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^([a-zA-Z0-9_-]+)$ index.php?slug=$1 [L,QSA]
